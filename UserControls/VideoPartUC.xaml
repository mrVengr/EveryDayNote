<UserControl x:Class="UserControls.VideoPartUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Padding="2"
             d:DesignHeight="300" d:DesignWidth="300" VerticalAlignment="Top" Height="330"
>
    <Grid TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}">
            <Button x:Name="AddBtn" Content="Add" HorizontalAlignment="Right" Margin="5" VerticalAlignment="Bottom" Width="75" FontFamily="Tahoma" FontWeight="Bold"/>
        <Border BorderThickness="3" Margin="10,10,10,60" CornerRadius="5">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="4"/>
            </Border.Effect>
            <MediaElement Name="video" DataContextChanged="video_DataContextChanged" />
            </Border>
        </Grid>
    
</UserControl>
