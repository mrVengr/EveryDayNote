<UserControl x:Class="UserControls.TextPartUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Padding="2"
             d:DesignHeight="300" d:DesignWidth="300" VerticalAlignment="Top" Loaded="UserControl_Loaded">
    <Grid TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}">
        <Border  BorderThickness="3" Margin="10,10,10,10" CornerRadius="5">
            <TextBox  Name="TextData" AcceptsReturn="True" TextWrapping = "Wrap" BorderThickness="1" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" TextChanged="TextData_TextChanged" VerticalScrollBarVisibility="Auto"/>
        </Border>
    </Grid>
</UserControl>
